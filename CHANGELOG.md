# Changelog

Notable changes to PRS Apps, newest first. Each entry is a date heading (`## YYYY-MM-DD`) followed by 1–2 line bullets. Routine/trivial changes live in git history, not here.

## 2026-07-31

- **`src/middleware.js` → `src/proxy.js`:** migrated to Next 16's renamed convention (exported fn `middleware` → `proxy`; matcher/logic unchanged) — clears the dev-server deprecation warning. Public webhook routes must now be excluded in `proxy.js`'s matcher.
- **Price Update Processor — ambiguous auto-pick + per-vendor match memory (migration `13`):** ambiguous lines (part number hits >1 P21 item) are no longer resolved by hand: the match route auto-assigns the closest candidate (`pickBestCandidate()` — exact supplier cross-ref > exact prefix-bridge id > has-prefix > has-cost, deterministic ties) with real old cost/Δ%/guardrail flag, included by default, reviewable at a glance in the Ambiguous tab; they no longer block Approve (only a pick-less ambiguous line does). The Flagged tab/count is now **matched-only** — ambiguous never mixes in. Approving (or manually fixing a line) records the resolution in the new `pu_item_aliases` table, which the match route consults **first** on the vendor's next batch, so previously-resolved parts match automatically. **Run `13_priceupdates_match_memory.sql`.** Also: export now leaves a truly blank cell on cost-only/list-only lines (P21 reads blank as "no change") instead of restating the mirror value.
- **Price Update Processor — Phase 5 (export + applied loop-closing):** new `POST /api/priceupdates/export` generates the P21-ready import file from an approved batch's **included + matched** lines: tab-delimited `.txt` (CRLF, header row) matching the template `price-update-processor/samples/GAT2026.txt` (gitignored — real pricing data, lives locally) — fixed column order `Item ID, List Price, New Cost, Supplier ID` (P21 imports by column position, not name), named `<vendor prefix><effective year>.txt`. File lands in `price-files` (`exports/<batchId>/`) + a `pu_exports` row + immediate download; status → `exported` (regenerate allowed). Batch detail gains Generate/Regenerate export, an Exports card, **Mark applied in P21** (stamps `applied_at/by`) and **Archive batch**; the queue gains a History (applied + archived) filter. No migration.
- **Price Update Processor — Phase 4 (review & approval UI):** the batch detail page is now a two-pane review screen. Left: editable line grid — All/Flagged/Unmatched/Excluded tabs with live counts, server-side pagination (100/page), inline edit of new cost/list (Δ% + flag recompute via shared `costChangePct`/`computeFlag`), a supplier-scoped mirror-backed item-search modal to fix unmatched/ambiguous lines (or clear a match), per-line + bulk include/exclude. Right: properties sidebar — vendor/status/counts, compact files card (auto-parse/map/download carried over), Received→Parsed→Approved→Exported→Applied timeline, and the **Approve** action, gated on zero included-ambiguous lines (re-verified at click), stamping `approved_by/at`+`reviewed_by` and logging an activity line to `notes`. Grid locks read-only once the batch leaves `needs_review`. No migration. _Post-review tweaks: Ambiguous got its own tab (it's the review-priority queue), Unmatched shows only true unmatched, and the match route now auto-excludes unmatched lines (re-including on an unmatched→matched transition, e.g. after a mirror sync; reviewer include/exclude choices are otherwise never touched on re-runs)._
- **Price Update Processor — RLS policies evaluated per row (fixed, migration `12`):** the `pu_*` policies called `has_app_access('priceupdates')` bare, so Postgres ran the profiles lookup per row — six concurrent tab-count queries over an 18k-line batch blew the `authenticated` statement timeout and randomly returned 500/503 (symptom: review-page tab counts showing 0). `12_priceupdates_rls_perf.sql` recreates the same policies with `(select has_app_access(...))` (init-plan, once per query). **Run `12` in the Supabase SQL Editor.** Diagnosed live in the browser via console/network inspection; verified all five tab counts populate consistently after. **Convention for future per-app-gated tables: always write RLS policies as `(select has_app_access('app'))`, never the bare call.**
- **Price Update Processor — match route only saw the first 1,000 lines (fixed):** the `pu_lines` fetch in `/api/priceupdates/match` wasn't paginated, so PostgREST's 1,000-row cap silently limited matching on large files (a real 18,671-line Gates file matched only 928). Now pages like the mirror fetch. Also set Gates' `p21_item_prefix` to `"GAT "`. Verified: same batch now matches 16,634 — in line with the manually-established ground truth (~2k unmatched lines confirmed absent from P21 in any format).

## 2026-07-30

- **Price Update Processor — P21 mirror sync unblocked via SQL replica:** the item-mirror sync now reads Epicor's hosted **read-only SQL Server replica** (new server-only client `src/lib/p21sql.js`, `mssql` dep, `P21_SQL_*` env) instead of the OData API, which remains 401-blocked at the gateway (diagnosed: token + catalog + $metadata OK for two users **and** a registered `/odata`-scope consumer key, but every data query denied — Epicor-side grant still pending). `/api/p21/sync-items` auto-prefers SQL when configured and keeps OData as the fallback; one joined query per supplier (same views/fields, env-overridable, columns verified against the replica), paged 5k with chunked deduped upserts. Replaced the placeholder seed `pu_vendors.p21_supplier_id`s with the real ones looked up in the replica (Gates 10638, ContiTech 10629, Parker 10140). **Verified live: full sync upserted 71,503 mirror rows in ~27s.** The replica is publicly reachable, so the nightly Vercel cron architecture stands. Also: Next.js bumped to 16.2.12; `SETUP-NEW-MACHINE.md` gained the git-identity step.

## 2026-07-29

- **Price Update Processor — P21 auth working; blocked on API-access grant:** hardened the P21 token client — the V2 `/api/security/token/v2` endpoint takes credentials in the JSON **body** (not headers) with `Content-Type`/`Accept: application/json` (header creds is deprecated V1 and NPEs); added XML-response fallback, `ExpiresInSeconds`, and an optional `P21_CONSUMER_KEY` client-credentials path. Reworked `/api/p21/sync-items` to mirror Porter's report query (join `p21_view_inventory_supplier` × `p21_view_inv_mast` on `inv_mast_uid`, scoped per tracked supplier, `delete_flag='N'`) and added a **Test connection** button on Settings. Against the play instance the token now succeeds; OData returns 401 "not authorized to access API" pending a P21-side grant for the `PRICEUPDATE` user. Evaluating a read-only SQL replica as a simpler mirror source. New-device/handoff notes in `price-update-processor/HANDOFF.md`.

## 2026-07-23

- **Price Update Processor — Phase 3 (P21 client, item-mirror sync, matching):** every parsed line now knows its P21 item + old cost. Migration `11_priceupdates_matching.sql` adds `pu_vendors.p21_item_prefix` and a `pu_apply_matches(jsonb)` bulk-update fn (leaves the reviewer's `include` flag alone). New generic P21 OData client `src/lib/p21.js` (token cache/refresh, retry/backoff, paged GET; all paths/views/fields env-configurable — reuse for future P21 apps). New route `POST/GET /api/p21/sync-items` (auth: `CRON_SECRET` cron **or** logged-in `priceupdates` user; added to `vercel.json` nightly cron + `middleware.js` exclusion) upserts item + supplier-cost data into `p21_item_mirror`. New route `POST /api/priceupdates/match` matches `vendor_item_no` → mirror `supplier_part_no` **and** prefix-stripped `p21_item_id`, scoped to the vendor's `p21_supplier_id` (1 hit = matched w/ old cost/list + Δ% + flag; >1 = ambiguous; 0 = unmatched); flags from `pu_settings` via `computeFlag()` (priority large_increase > cost_over_list > decrease > ok). Matching runs after parse and via a **Re-run matching** button; the batch line preview now shows old→new cost, Δ%, flag dot, match pill. **Settings** page: real P21 mirror stats + "Sync now". **Vendors** page: minimal add/edit of P21 supplier id + item prefix + email domains (matching prerequisite; full CRUD is Phase 7). Pure helpers (`normalizePart`/`costChangePct`/`computeFlag`) in `src/lib/priceupdates.js`; client triggers (`triggerMatch`/`syncP21`) in `src/lib/priceupdatesParse.js`. New env: `P21_BASE_URL`/`P21_USERNAME`/`P21_PASSWORD` (+ optional view/field overrides). **Run `11` in the Supabase SQL Editor; set each vendor's P21 supplier id + prefix; add P21 creds to the environment; then Sync now.** _Verified: matching/flag helpers 17/17 unit assertions incl. the real `GAT ` prefix bridge. The live P21 OData connection is unverified until creds + real view names are confirmed._
- **Price Update Processor — Phase 2 (parsing engine + column-mapping UI):** spreadsheets now become `pu_lines` without hand-editing in Excel. New server route `POST /api/priceupdates/parse-file` (Bearer-auth + `priceupdates` access check, mirrors `/api/helpdesk/notify`) downloads a `pu_batch_files` row from `price-files` via the service role and reads it with SheetJS (`xlsx`; handles .xlsx/.xls/.csv) into raw 2-D rows (`raw:false` so cells arrive as formatted text). Pure transform helpers in `lib/priceupdates.js` — `parseNumber` (strips `$`/commas/accounting-parens; blank≠zero), `parseDateish`, and `buildLinesFromRows(rows, config)` (applies multiplier / discount-%-off-list / strip-prefix, stores the untouched row in `raw`, skips+counts no-price lines). Client helpers in new `lib/priceupdatesParse.js` (`fetchParsedSheets`, `applyParse` — writes lines, marks the file `parsed`, recomputes batch counts, advances `received/parsing/failed → needs_review`, optionally saves a `pu_parse_profiles` row). New mapping UI `/priceupdates/batches/[id]/map`: 30-row grid with click-to-pick header row, auto-guessed header/columns, per-field column dropdowns, transforms, a live preview of parsed lines + skipped count, and "save as parse profile". Batch detail page gained per-file parse status, a one-click **Auto-parse (profile)** when the vendor has a saved profile (fallback to the mapping UI on failure/none), and a read-only parsed-lines preview. No migration (all tables shipped in `10`). _Judgment call: auto-parse is an explicit one-click button, not a silent on-load write — automatic on-arrival parsing is Phase 6's email path._
- **Price Update Processor — Phase 1 (new app `priceupdates`):** first slice of the app that replaces the retired Invoice Processor's price-update duty and will integrate with Prophet 21. Migration `10_priceupdates_schema.sql` adds the full data model (`pu_vendors`, `pu_parse_profiles`, `pu_batches`, `pu_batch_files`, `pu_lines`, `p21_item_mirror`, `pu_exports`, `pu_settings` + 5 enums), RLS via a new `has_app_access(app)` SQL helper (`is_agent()` OR `app_access ? app`), the private `price-files` Storage bucket + policies, and seed vendors + a sample batch. New app id `priceupdates` (default **off** for everyone except the owner) registered in nav/home/admin and guarded once in `src/app/priceupdates/layout.js` (Help Desk-style icon rail: Dashboard, Batches, Vendors, Settings). Dashboard (metric cards + recent batches), batch queue (`/priceupdates/batches`, status + attention pills, filters), a **New batch** modal (pick/create vendor + drag-drop files → `pu_batches`/`pu_batch_files`, status `received`), and a minimal batch detail page (header + attached files). Shared helpers in `src/lib/priceupdates.js`. Parsing, P21 matching, review/approve, export, and email intake come in later phases. **Run `10` in the Supabase SQL Editor to apply.**

## 2026-07-20

- **IT Tracker — rack-mountable assets aren't assignable:** in `AssetModal`, checking **Rack-mountable** now greys out/disables the **Assigned To** picker and clears any assignee (rack gear is infrastructure, not assigned to a person); the save always nulls `assigned_employee_id`/`assigned_to` for rack-mountable assets. The **Check Out** button in `AssetDetail` is also hidden for rack-mountable assets to close the other assignment path.
- **IT Tracker — Stage 5 (rack audit workflow):** migration `09_rack_audits.sql` adds `rack_audits` + `rack_audit_items` (RLS on). Starting an audit from the rack detail page snapshots the currently-mounted devices as expected items; the auditor then walks the rack marking each **Present / Moved / Missing** (with a found-at-U for moved), logs **unexpected devices found**, records per-item **condition** + notes and overall notes, then **Completes** the audit (or Discards an in-progress one). Completed audits are read-only. The rack page gained an **Audits** section (last-completed date + history list with status, auditor, and discrepancy count) and a **Start/Resume Audit** button. Snapshots preserve device name/type/expected slot so history survives later asset edits/deletes. Shared audit helpers (`AUDIT_RESULTS`, `AUDIT_CONDITIONS`, `countDiscrepancies`) in `lib/tracker.js`. **Reconciliation:** findings drive the live data — an unexpected ("extra") device has a **Create asset** action that opens `AssetModal` prefilled (name + rack + found-at-U) and links the new asset back to the finding; a **moved** device has an **Apply move → U#** action that repositions the real asset (overlap-validated); a **missing** device has a **Remove from rack** action (detaches `rack_id`/`u_position`, confirm-guarded). `AssetModal` gained a `defaults` prop to seed a new asset. Note: `rack_audit_items.asset_id` is `bigint` (matches the legacy `assets.id`, not uuid). **Run `09` in the Supabase SQL Editor to apply.**
- **IT Tracker — rack visualization redesign (Lucidchart-style):** the rack detail grid now looks like a physical server rack — a metal enclosure with a title bar and two mounting rails dotted with holes, a left U-number gutter, and **per-type device faceplates** (new `RackDeviceFace` component): switch/router/firewall show port rows, servers show a vent grille, Storage/NAS shows drive bays with activity LEDs, UPS shows round outlets, Monitor is a blue screen, everything else a generic plate — each with a status LED and a name chip that appears on hover. Interactions unchanged (click a device to edit, hover to Unmount, click an empty slot to place). `ROW_PX` bumped to 30.
- **IT Tracker — rack-mountability is now an explicit flag (Stage 4 follow-up):** migration `08_asset_rack_mountable.sql` adds `assets.rack_mountable` (boolean, backfilled true for the previously type-rackable set + anything already in a rack). The asset form now has a **Rack-mountable device** checkbox that gates the Rack/Power block, so *any* type — including "Other" — can be racked; `type` still drives which detail fields show (computer specs vs. management URL). Picking a type suggests a default for the checkbox on new assets only. Added an **overlap/fit safeguard**: saving a rack-mountable asset with a rack + U position is rejected if it exceeds the rack or collides with another device there (shared `rackPlacementError()` in `lib/tracker.js`, used by both `AssetModal` and `PlaceDeviceModal`). **Run `08` in the Supabase SQL Editor to apply.**
- **IT Tracker — Stage 4 (racks + U-position visualization):** new Racks app section — list page (`/tracker/racks`, per-rack device count / U used / total power) and a rack detail page (`/tracker/racks/[id]`) with a **to-scale, clickable U grid** (highest U at top, U1 at bottom; a device spans `u_height` U's from its `u_position`). Click a mounted device to edit it (`AssetModal`) or **Unmount** (keep in rack, `u_position` null); click an empty slot to open **`PlaceDeviceModal`** (pick a rackable unmounted device + bottom U, validated for fit/overlap). Devices assigned to a rack with no `u_position` show in an **Off-rack (in room)** column. Total power is auto-summed from device `watts` (not stored). `AssetModal`'s Rack/Power block gained a **Rack** picker (`rack_id`; clearing it clears `u_position`); `AssetDetail` shows the rack name + "Off-rack". New `RackModal` (name, cascading Location→Room, size in U, notes). Registered **Racks** in the tracker nav. No migration — `racks` existed from `05`, `room_id` from `07`.
- **IT Tracker — Stage 3 (asset type + type-aware detail):** `AssetModal`/`AssetDetail` gained a **Type** field (`ASSET_TYPES`) that drives which detail fields show. Computer types (Laptop/Desktop/Server) show a **Computer Details** block (hostname, IP, OS, CPU, RAM, storage); non-computer types show an optional **Management URL**; rack-mountable types (Server/Switch/Router/Firewall/NAS/UPS) show a **Rack / Power** block (watts, U-height, rack position). Added a `rackable` flag + `isRackableType()` to `src/lib/tracker.js` (the memory's "type drives rack eligibility"). On save, detail fields not applicable to the chosen type are nulled so rows stay consistent; the assets list now shows the type next to each serial. No migration — all columns existed from `05`.
- **IT Tracker — Stage 2 (employee detail + grouped asset views):** new employee detail page (`/tracker/employees/[id]`) showing the employee's info (email, department, title, location/room, manager) + a table of all assets assigned to them; employees-list rows now navigate there, and editing moved onto the detail page (the list keeps only "+ Add Employee"). Assets list gained a **Group by** control (None / Employee / Location) rendering one table per group with the catch-all bucket (Unassigned / No location) last. `AssetModal`'s free-text Location input replaced with cascading **Location → Room** dropdowns writing `location_id`/`room_id` (mirrors the employee form; pre-FK free-text shown as a migrate hint); asset detail now shows the FK location/room. No migration — `assets.location_id`/`room_id` already existed from `05`/`07`.

## 2026-07-17

- **IT Tracker — location → room hierarchy (Stage 1 rework):** migration `07_location_rooms.sql` adds a `rooms` table (belongs to a location) and `room_id` on employees/assets/racks; retires the flat `locations.site`/`locations.room` text columns. Locations list row now opens a **location detail page** (`/tracker/locations/[id]`) that manages that branch's rooms (`RoomModal`, add/edit/delete with in-use warning). `LocationModal` simplified to name/address/notes. Employee form now has cascading **Location → Room** dropdowns (room list filtered to the chosen branch; changing branch clears the room).
- **IT Tracker — Stage 1 follow-ups:** employee name split into `first_name`/`last_name` (migration `06_employee_names.sql`, backfilled from `full_name`, which the app still writes as the combined value); employee form now has separate first/last fields. Brought the **Locations** UI forward from Stage 2: `/tracker/locations` page + `LocationModal`, registered in nav; the employee form gained a **Location / Branch** dropdown (`location_id`) for multi-branch companies, surfaced as a column on the employees list.
- **IT Tracker asset management — Stage 1 (employees + real assignment):** new `/tracker/employees` directory page (list, search, active/inactive filter, per-employee asset count) with `EmployeeModal` add/edit (name, email, department, title, manager, status, notes). Registered in the tracker nav. Assignment moved off the free-text `prompt()` to a real employee FK: checkout now uses `CheckoutModal` (active-employee picker) writing `assigned_employee_id`; `AssetModal`'s "Assigned To" is an employee dropdown (shows a legacy-value hint for pre-FK rows). `assigned_to` is kept as a cached display/search name synced to the chosen employee. Assets list/detail resolve the assignee via an `employees(full_name)` embed.
- **IT Tracker asset management — Stage 0 (foundation):** first committed tracker DDL `supabase/05_tracker_schema.sql` (idempotent, RLS-on). Adds `locations`, `employees`, `racks` tables; asset columns for `type`, rack/device fields (`rack_id`, `u_position`, `u_height`, `watts`, `hostname`, `ip_address`, `os`, `cpu`, `ram`, `storage`, `management_url`), and relational `assigned_employee_id` / `location_id`. New **Asset ID** system: `PRS-#####` global sequence, immutable, auto-assigned on insert, existing rows backfilled oldest-first; the ID doubles as the printed asset tag. Added `src/lib/tracker.js` (shared statuses/categories/types/colors/formatters/depreciation) and `src/app/tracker/layout.js` (single `hasAccess('tracker')` guard for all `/tracker/*` pages — previously only the dashboard guarded). Asset components refactored onto the shared lib. **Run `05` in the Supabase SQL Editor to apply.**
- **Dev onboarding:** added `.env.example` (env-var template, no secrets; `!.env.example` un-ignored) and a "Setting up on a new machine" section in `README.md` (prereqs → clone → `.env.local` → run).
- **Agent config:** committed `.claude/settings.json` — auto-approves routine tool use, keeps `git add/commit/push` behind an `ask` prompt, and denies destructive commands (recursive deletes, `git reset --hard`, force-push, etc.).

## 2026-07-16 — Help Desk launch (replaces Invoice Processor)

- **Help Desk app** (`helpdesk`): Freshservice-style dashboard, dense ticket queue (SLA state / status / priority / assignee, filters), two-pane ticket detail, and an agent-logs-on-behalf "New ticket" form. Registered in nav, home tiles, and admin access.
- **SLA:** priority-based `sla_policies` stamp first-response/resolution due times on insert and recalculate on priority change (DB triggers); queue/detail show derived state (overdue / due today / on time).
- **Knowledge Base:** browse + search, markdown article view with helpful votes, and agent CRUD. "Document this resolution" links a resolved ticket to a similar article (pg_trgm + full-text `search_kb`) or opens a prefilled new article; articles list their related tickets.
- **Categories:** editable Category dropdown on tickets (changes logged to Activity); shared category list including "Price update".
- **Attachments:** file upload to Supabase Storage on tickets, download via signed URLs.
- **Email via Microsoft Graph (no Power Automate):** inbound push webhooks turn emails to `it@` / `priceupdate@` into tickets (`priceupdate@` → `price_update` category), de-duplicated by message id; outbound replies and assignment notices sent as `it@`. Daily Vercel cron renews Graph subscriptions.
- **Invoice Processor:** slated for retirement via the `app_access`/registry soft-hide — see `docs/deprecation-checklist.md`.
- **Repo/portability:** added `AGENTS.md` project guide; moved SQL migrations into `supabase/` (numbered, idempotent) and planning/setup docs into `docs/`.
