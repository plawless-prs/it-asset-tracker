'use client'

// Placeholder — full vendor CRUD (p21_supplier_id, email domains, saved parse
// profiles) lands in Phase 7.
export default function VendorsPlaceholder() {
  return (
    <div style={{ padding: '24px 28px', maxWidth: '900px' }}>
      <h1 style={{ fontSize: '22px', fontWeight: '700', color: '#e0e7f0', margin: '0 0 4px' }}>Vendors</h1>
      <p style={{ fontSize: '13px', color: '#5a6e84', margin: '0 0 24px' }}>
        Vendor directory and saved parse profiles.
      </p>
      <div style={{
        backgroundColor: '#0f1620', border: '1px solid #182030', borderRadius: '14px',
        padding: '40px', textAlign: 'center', color: '#4a5a6e', fontSize: '13.5px',
      }}>
        Vendor management arrives in a later phase.
      </div>
    </div>
  )
}
